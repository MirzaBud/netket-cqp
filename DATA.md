# Data and results

`data/time_dof/` contains the compact timing measurements used for the scaling plots.
`results/netket_outputs/` contains the curated `.npz` outputs used by the notebooks.

Files produced by new experiments should be stored under `runs/` or `checkpoints/`.
Those folders are intentionally excluded from Git; move only selected, documented
results into `results/`.
