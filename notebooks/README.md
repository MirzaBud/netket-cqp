# Notebooks

Run notebooks from the repository root so relative paths resolve consistently:

```bash
jupyter lab
```

The original exploratory notebooks are preserved here. `main.ipynb` uses the
installed `netket_cqp` package and writes new experiment files to
`results/netket_outputs/`. The plotting notebooks read from that same directory.
